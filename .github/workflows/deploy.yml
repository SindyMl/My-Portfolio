name: Manual Deployment Info

on:
  workflow_dispatch:

jobs:
  deployment-info:
    runs-on: ubuntu-latest
    steps:
      - name: Show Deployment Instructions
        run: |
          echo "Portfolio uses manual deployment with gh-pages package"
          echo "To deploy updates, run: npm run deploy"
          echo "Live site: https://sindyml.github.io/My-Portfolio/"